export type SelectFilters = {
    SELECT_CALLER: string;
    SELECT_CALLEE: string;
}

const selectFilters: SelectFilters = {
    SELECT_CALLER: 'Звонящая сторона(Caller)',
    SELECT_CALLEE: 'Принимающая сторона(Callee)',
};

export default selectFilters;